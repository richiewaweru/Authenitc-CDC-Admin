<script lang="ts">
	import PlaceholderPage from '$lib/components/PlaceholderPage.svelte';

	const notes = [
		'Calendar-first schedule with status colors from the design system.',
		'Week navigation, guide filtering, and a list-view toggle.',
		'Guide-scoped read-only experience with staff publishing controls.'
	];
</script>

<PlaceholderPage
	title="Slots"
	description="The protected route and sidebar visibility are live, so Milestone 4 can focus purely on slot UX and data."
	{notes}
/>
